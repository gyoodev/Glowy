// Placeholder Logo component to resolve build error
// Please replace this with your actual Logo component if src/components/Header/Header.js is in use.
'use client';

import { Sparkles } from 'lucide-react';
import type { FC } from 'react';

const Logo: FC = () => {
  // Using Sparkles icon as a placeholder
  return <Sparkles className="h-6 w-6 text-primary" />;
};

export default Logo;
